import express, { Router, Request, Response } from 'express';
const router = Router();

//***** SOBRE CONEXÃO
//const conn1 = require('../db/connection'); //?assim ele executa quando importa
import { conn } from '../db/connection'; //!assim o modulo não é chamado ao ser importado, até que seja explicitamente chamado
conn; //!com essa chama que ai ele executa
//***** SOBRE CONEXÃO

//????????????????????????????????????????????????????????????????? configuracoes
router.use(express.urlencoded({
  extended: true
}));
router.use(express.json());
//????????????????????????????????????????????????????????????????? configuracoes

//! base route
router.get('/', (req: Request, res: Response) => {
  res.render('home');
});

export default router;